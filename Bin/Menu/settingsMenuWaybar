#!/bin/sh 

choice=$(printf " Back\n Config\n Style" | rofi -dmenu -p "")

case "$choice" in
    " Back")
        sh ~/bin/Menu/settingsMenu
        ;;
    " Config")
        kitty -e nvim ~/.config/waybar/config.jsonc
        ;;
    " Style")
        kitty -e nvim ~/.config/waybar/style.css
        ;;
    *)
esac
